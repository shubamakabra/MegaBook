# LLM Provider: 'mock' for testing, 'azure' for Azure OpenAI
LLM_PROVIDER=mock

# Azure settings (only needed if LLM_PROVIDER=azure)
AZURE_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_API_KEY=your-api-key
AZURE_DEPLOYMENT=gpt-4

# Cost limit in Norwegian Kroner
COST_LIMIT_NOK=10